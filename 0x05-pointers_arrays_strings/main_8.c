#include "main.h"
#include <stdio.h>

/**
* main - Entry point of the program
*
* Description: This function serves as the entry point for the program. It
* demonstrates the usage of the _atoi function.
*
* Return: Always returns 0.
*/
int main(void)
{
int nb;
nb = _atoi("402"); // Corrected input string
printf("%d\n", nb);
return (0);
}

